#!/usr/bin/env python

class Laser:
        
        def __init__(self, ion_number, freq, intensity):
                self.ion_number   = ion_number
                self.freq         = freq
                self.intensity    = intensity
                
                